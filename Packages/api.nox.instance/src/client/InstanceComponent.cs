using UnityEngine;

namespace api.nox.instance.client {
	public class InstanceComponent : MonoBehaviour {
		public static (GameObject, InstanceComponent) Generate(InstancePage userPage, RectTransform parent)
			=> (null, null);
	}
}